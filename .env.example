# Local demo only. Do not commit real secrets.
POSTGRES_USER=anomaly
POSTGRES_PASSWORD=anomaly_demo
POSTGRES_DB=anomaly
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

KAFKA_BOOTSTRAP_SERVERS=redpanda:9092
KAFKA_CLIENT_BOOTSTRAP_SERVERS=localhost:19092

BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
STREAMING_METRICS_PORT=8001
STREAMING_HEALTH_PORT=8002

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Optional: override artifact path
ML_ARTIFACT_DIR=/app/ml/artifacts
